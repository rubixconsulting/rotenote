#SET(CMAKE_CXX_FLAGS "-Wall -W -O2 -fPIC")
SET(CMAKE_CXX_FLAGS "-Wall -W -O0 -g")
FILE(GLOB HEADERS "*.h")
FILE(GLOB SOURCES "*.cpp")
ADD_EXECUTABLE(client_cli ${SOURCES})
TARGET_LINK_LIBRARIES(client_cli rote)
INSTALL(TARGETS client_cli DESTINATION bin)
